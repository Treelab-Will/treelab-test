/** @type {import('next').NextConfig} */
module.exports = {
  reactStrictMode: true,
  // 运行时环境变量请查看： https://nextjs.org/docs/api-reference/next.config.js/runtime-configuration
  // Will only be available on the server side
  serverRuntimeConfig: {},
  // Will be available on both server and client
  publicRuntimeConfig: {},
};
